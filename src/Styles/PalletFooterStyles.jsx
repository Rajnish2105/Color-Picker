import { styled } from "@mui/system";

export const PalletFooterContainer = styled("footer")({
  backgroundColor: "white",
  height: "5vh",
  display: "flex",
  justifyContent: "flex-end",
  alignItems: "center",
  fontWeight: "bold",
});

export const Emoji = styled("span")({
  fontSize: "1.5rem",
  margin: "1rem",
});